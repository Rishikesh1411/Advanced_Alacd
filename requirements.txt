# requirements.txt
# modified by rishikesh
#
# OLD CODE:
# # No requirements.txt existed in this workspace, so reviewers could not
# # recreate the ALACD environment reliably.
#
# NEW CODE:
# Core runtime plus optional research-validation metrics requested in the
# problem-analysis PDF. Install with:
#   pip install -r requirements.txt

torch>=2.2.0
transformers>=4.41.0
accelerate>=0.30.0
datasets>=2.19.0
huggingface-hub>=0.23.0
tokenizers>=0.19.0
safetensors>=0.4.3

numpy>=1.24.0
pandas>=2.0.0
scipy>=1.10.0
tqdm>=4.66.0
scikit-learn>=1.3.0

rouge-score>=0.1.2
bert-score>=0.3.13
nltk>=3.8.1

wandb>=0.16.0
tabulate>=0.9.0

# Optional GPU speedup. Install only when your CUDA/PyTorch build supports it:
# flash-attn>=2.5.0

# NEW CODE by Rishikesh: optional task-specific quality measures.
# Only needed if you pass --use-comet / --use-alignscore to main.py.
# unbabel-comet>=2.2.0
# alignscore>=0.1.3
